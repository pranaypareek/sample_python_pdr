language: python

python:
  - 3.4

env:
  global:
    - FOO=FUBU

  matrix:
    - M1=ONE

build:
  #pre_ci:
   # - docker build -t=quay.io/shippablequay/hitchcock2_sample_python .

 

  ci:
    - ps -eax
    - ls -al
    - sudo apt-get update

  post_ci:
    - docker build -t=pranay/hitchcock2_sample_python .
    - docker push pranay/hitchcock2_sample_python
    - docker build --rm -t=siddarthan/sample_python_pdr:test .
    - docker build --rm -t=amalfra/sample_python_pdr:qa .
    - docker push siddarthan/sample_python_pdr:test
    - docker push amalfra/sample_python_pdr:qa


integrations:
  hub:
    - integrationName: docker
      type: docker
    - integrationName: docker-siddarthan
      type: docker
    - integrationName: docker-amal
      type: docker
      
